{
  "name": "lazy-forms",
  "version": "1.1.1",
  "description": "Context-aware storage for form field values (input, textarea, select). Right-click to store or apply values.",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "node scripts/build-unpacked.js",
    "build:chrome": "npm run manifest-chrome && bestzip ./dist/lazy-forms.zip manifest.json background.js content.js lib/ sidepanel/",
    "build:firefox": "npm run manifest-firefox && web-ext build --overwrite-dest --artifacts-dir dist",
    "manifest-chrome": "node -e \"require('fs').copyFileSync('manifest.chrome.json','manifest.json')\"",
    "manifest-firefox": "node -e \"require('fs').copyFileSync('manifest.firefox.json','manifest.json')\"",
    "lint": "eslint .",
    "format": "prettier --write .",
    "lint:firefox": "web-ext lint"
  },
  "keywords": [
    "chrome-extension",
    "forms",
    "autofill",
    "browser-extension"
  ],
  "author": "",
  "license": "MIT",
  "devDependencies": {
    "bestzip": "^2.2.1",
    "eslint": "^9.39.0",
    "web-ext": "^9.2.0"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}
